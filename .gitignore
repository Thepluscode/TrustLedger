# Java / Maven
target/
build/domain-validation/classes/
*.class

# Node / Next.js
node_modules/
.next/
out/
dist/

# Env / secrets
.env
.env.*
!.env.example
*.tfstate
*.tfstate.*

# OS / editor
.DS_Store
*.log

# tool/session artifacts
.playwright-mcp/
council-out/

# Generated by `make project-state`. Never committed: a state file that records the
# commit adding it is stale from the moment it lands, and it mixes one machine's local
# test artefacts with facts any clone can derive. Regenerate it, do not store it.
PROJECT_STATE.json
__pycache__/
